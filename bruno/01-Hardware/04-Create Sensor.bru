meta {
  name: Create Sensor
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/hardware/sensors
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "serial": "soil_probe_zone_a",
    "name": "Zone A Soil Probe",
    "parameter_ids": ["soil_moisture_pct"],
    "api_url": "",
    "update_frequency_seconds": 60
  }
}
