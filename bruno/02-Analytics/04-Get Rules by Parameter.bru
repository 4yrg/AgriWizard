meta {
  name: Get Rules by Parameter
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/analytics/rules/{{thresholdParameterId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
